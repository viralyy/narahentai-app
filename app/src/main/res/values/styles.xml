<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base App Theme -->
    <style name="Theme.Narahentai" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Background utama -->
        <item name="android:windowBackground">#0B0B0F</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">#0B0B0F</item>
        <item name="android:navigationBarColor">#0B0B0F</item>

        <!-- Text color default -->
        <item name="android:textColorPrimary">#FFFFFF</item>
        <item name="android:textColorSecondary">#BDBDBD</item>

        <!-- Disable default actionbar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

</resources>
